<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.esi.navigator_22">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/navigator_22"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/navigator_22"
        android:supportsRtl="true"
        android:theme="@style/Theme.Navigator22"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".AllNearTramwayStationsActivity"
            android:theme="@style/Theme.AppTheme"/>

        <activity
            android:name=".NthTramwayStationsActivity"
            android:theme="@style/Theme.AppTheme"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>